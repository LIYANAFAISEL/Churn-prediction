server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Proxy API calls to live Render API
    location /api/ {
        proxy_pass https://churn-api-wgoc.onrender.com/;
        proxy_set_header Host churn-api-wgoc.onrender.com;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}